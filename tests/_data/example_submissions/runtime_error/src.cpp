#include <stdio.h>
using namespace std;

//adds two numbers
int main(int argc, char **argv){
    char arr[1];
    int idx = 0;
    while (true){
        printf("%c", arr[idx++]);
        fflush(stdout);
    }
}
